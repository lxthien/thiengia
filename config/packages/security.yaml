security:
    enable_authenticator_manager: true

    password_hashers:
        App\Entity\User: bcrypt

    role_hierarchy:
        ROLE_AUTHOR:      ROLE_USER
        ROLE_EDITOR:      ROLE_AUTHOR
        ROLE_ADMIN:       ROLE_EDITOR
        ROLE_SUPER_ADMIN: ROLE_ADMIN

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username

    firewalls:
        main:
            pattern: ^/
            provider: app_user_provider
            user_checker: App\Security\AppUserChecker
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true

            logout:
                path: app_logout

            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800

            login_throttling:
                max_attempts: 5
                interval: '15 minutes'

    access_control:
        - { path: ^/login$, roles: PUBLIC_ACCESS }
        # Chỉ chặn ở ngưỡng thấp nhất (ROLE_AUTHOR) — phân quyền chi tiết hơn theo
        # từng controller nằm ở #[IsGranted(...)] cấp class/method (xem src/Controller/Admin/*).
        - { path: ^/admin/, roles: ROLE_AUTHOR }
