{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_redirect_new' %}

{% block main %}
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title">Create Redirect</strong>
                        </div>
                        <div class="card-body">
                            {{ include('admin/redirect/_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
