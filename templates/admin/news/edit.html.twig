{% extends 'admin/layout.html.twig' %}

{% block admin_editor_stylesheets %}
    <link rel="stylesheet" href="{{ asset('build/css/ckeditor5.css') }}">
{% endblock %}

{% block body_id 'admin_news_edit' %}

{% block main %}
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title">{{ 'title.edit_news'|trans({'%id%': news.id}) }}</strong>
                            <a class="pull-right" target="_blank" href="{{ path('dynamic_post_page', {'slug': news.url, 'preview' : 'true', 'preview_id' : news.id }) }}">Xem trước</a>
                        </div>
                        <div class="card-body">
                            {{ include('admin/components/seo-checker.html.twig', {form: form}, with_context = false) }}

                            <!-- Credit Card -->
                            <div id="pay-invoice">
                                <div class="card-body">
                                    {{ include('admin/news/_form.html.twig', {
                                        form: form,
                                        button_label: 'action.save'|trans,
                                        include_back_to_home_link: true,
                                    }, with_context = false) }}
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- .card -->
                </div>
                <!--/.col-->
            </div>
        </div>
        <!-- .animated -->
    </div>
{% endblock %}
