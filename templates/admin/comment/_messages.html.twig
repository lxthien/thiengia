{% for type, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ type in ['success','warning','danger','info'] ? type : 'info' }} comment-notice" role="alert">{{ message|trans }}</div>
    {% endfor %}
{% endfor %}
