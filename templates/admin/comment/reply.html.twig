{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_comment_new' %}

{% block body_class 'admin_new' %}

{% block main %}
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title">{{ 'title.comment_new'|trans }}</strong>
                        </div>
                        <div class="card-body">
                            <div id="pay-invoice">
                                <div class="card-body">
                                    {{ include('admin/comment/_form.html.twig', {
                                        form: form,
                                        button_label: 'label.create_news'|trans,
                                        include_back_to_home_link: true,
                                    }, with_context = false) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
