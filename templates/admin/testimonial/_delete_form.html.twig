<button type="button" class="banner-button banner-button--delete" data-testimonial-delete="{{ path('admin_testimonial_delete', {id: object.id}) }}" data-token="{{ csrf_token('delete') }}" data-name="{{ object.name }}" aria-label="Xóa đánh giá của {{ object.name }}"><i class="fa fa-trash-o" aria-hidden="true"></i> Xóa</button>
